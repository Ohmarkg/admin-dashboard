// Minimal placeholder so the post-login redirect lands somewhere. Real dashboard is task D1.
export default function DashboardPage() {
    return (
        <main className="flex min-h-screen items-center justify-center">
            <h1 className="text-2xl font-bold">Dashboard — coming in D1</h1>
        </main>
    );
}
